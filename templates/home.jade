extends ./layout/index.jade

block content
    .container
        .hero
            h4 Login
            a(href='/login' class='btn btn-default') Login
                span.fa.fa-user
                
            h4 Register
            a(herf='/signup' class='btn btn-default') Register
                span.fa.fa-user
                
        
